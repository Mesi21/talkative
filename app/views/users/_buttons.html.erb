<div class="card-body d-flex flex-row">
  <%= link_to 'View more posts', user_posts_path(@user), class: 'btn btn-lg btn-success' %>
  <%= link_to 'Back', users_path(@user), class: 'btn btn-lg btn-danger' %>
</div>